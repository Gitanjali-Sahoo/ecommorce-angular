export interface Product {
  id: number;
  name: string;
  price: string;
  image: string;
  sku: string;
  description: string;
  category: string;
  slug: string;
  createdAt: string;
  isNew: boolean;
  hide: boolean;
}
